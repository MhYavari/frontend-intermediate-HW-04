# Exercise 4 — Live Products (Fetch + async/await)

A small Vite + Tailwind app that fetches product data from the public
[DummyJSON](https://dummyjson.com/products) API and renders it as a grid of
cards, with visible Loading, Error, and Success states.

## Getting started

```bash
npm install
npm run dev
```
